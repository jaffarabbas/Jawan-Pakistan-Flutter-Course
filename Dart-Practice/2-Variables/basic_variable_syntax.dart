/*
var is a variable syntax that store every data type
*/

//<datatype name> <variable name> <assignment operator> <value>
var number = 3;
var number2 = 4;
//temp variable is a naming convention that is use for temprory variable
//temporary varibale: those variables that are use for storing values temporary
var temp;
void main() {
  temp = number + number2;
  print(number);
  print(temp);
}